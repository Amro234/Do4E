<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ── Notification & alarm permissions ─────────────────────────────── -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
        Required for fullScreenIntent to work on Android 11+ (API 30+).
        On Android 14+ the user must also grant this in Settings;
        the app checks and prompts inside MainActivity.onCreate().
    -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Needed to show MedAlarmActivity over the lock screen -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Do4E">

        <!-- ── Onboarding (launcher entry point) ──────────────────────── -->
        <activity
            android:name=".ui.Onboarding.onboarding_main"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.Onboarding.onboard_nav"
            android:exported="false" />

        <!-- ── Main app shell (NavHostFragment + BottomNav) ───────────── -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- ── Full-screen medication alarm UI ────────────────────────────
             Key flags:
               showWhenLocked  – display on top of the lock screen
               turnScreenOn    – wake the display when alarm fires
               launchMode      – singleInstance prevents stacking multiple
                                  alarm screens if several alarms fire close together
               excludeFromRecents – hide from the recents / task switcher
        -->
        <activity
            android:name=".ui.notification.med_alarm"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:launchMode="singleInstance"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.Do4E" />

        <!-- ── BroadcastReceivers ──────────────────────────────────────── -->
        <receiver
            android:name=".reminder.ReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".reminder.MedicineActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.do4e.ACTION_TAKEN" />
                <action android:name="com.example.do4e.ACTION_SKIP" />
            </intent-filter>
        </receiver>

    </application>

</manifest>